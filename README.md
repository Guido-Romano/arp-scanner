# arp-scanner
ARP Network Scanner is a Python script designed to scan local networks using ARP protocol. It automatically detects the Class C network based on the local IP and maps active devices efficiently.
